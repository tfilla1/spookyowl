{
  "name": "chat",
  "version": "1.0.0",
  "description": "a chat app for peace",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon ./index.js",
    "ui": "browser-sync start --proxy=localhost:8000 --files='**/*.css, **/*.pug, **/*.js' --ignore=node_modules --reload-delay 10 --no-ui --no-notify"
  },
  "author": "tfilla",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "nodemon": "^2.0.2"
  },
  "dependencies": {
    "bufferutil": "^4.0.1",
    "express": "^4.17.3",
    "socket.io": "^2.3.0",
    "utf-8-validate": "^5.0.2"
  }
}
